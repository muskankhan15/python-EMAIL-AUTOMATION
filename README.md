# python-EMAIL-AUTOMATION
This Python script sends an email using Gmail’s SMTP server with SSL encryption. It builds an EmailMessage object, sets sender, receiver, subject, and body, then securely logs into the Gmail account using an app password and sends the message through SMTP_SSL on port 465.
